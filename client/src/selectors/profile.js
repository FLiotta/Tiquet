export const selectProfile = state => state.profile;

export const selectUsername = state => state.profile.username;

export const selectCreatedAt = state => state.profile.createdAt;

export const selectId = state => state.profile.createdAt;